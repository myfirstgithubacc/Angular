import { ApiLoginResponse } from './api-login-response.model';

describe('ApiLoginResponse', () => {
  it('should create an instance', () => {
    expect(new ApiLoginResponse()).toBeTruthy();
  });
});
