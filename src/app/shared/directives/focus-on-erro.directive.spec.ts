import { FocusOnErrorDirective } from './focus-on-erro.directive';

describe('FocusOnErroDirective', () => {
	it('should create an instance', () => {
		const directive = new FocusOnErrorDirective();
		expect(directive).toBeTruthy();
	});
});
