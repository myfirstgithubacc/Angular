
import { NoSpaceDirective } from './no-space.directive';

describe('Directive: NoSpace', () => {
  it('should create an instance', () => {
    const directive = new NoSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
