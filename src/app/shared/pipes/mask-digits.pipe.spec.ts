import { MaskDigitsPipe } from './mask-digits.pipe';

describe('MaskDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
